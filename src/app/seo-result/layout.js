import { canonical } from "@/lib/seo-config";

export const metadata = {
  title: "SEO Audit Result | Klarai",
  description: "SEO audit result generated by Klarai.",
  robots: "noindex, nofollow",
  alternates: {
    canonical: canonical("/seo-result"),
  },
};

export default function SeoResultLayout({ children }) {
  return children;
}
